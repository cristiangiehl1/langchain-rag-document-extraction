# @langchain/community declares a peerOptional pdf-parse@1.1.1 that conflicts with
# our pdf-parse@^1.1.4. Both work together in practice; keep npm's pre-v7 resolution
# so `npm install` (local and on Vercel) doesn't fail with ERESOLVE.
legacy-peer-deps=true
